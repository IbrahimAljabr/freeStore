import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  root: {
    background: "white",
    position: "absolute",
    bottom: "0",
    width: "100%",
  },
  toolbar: {
    padding: "10px 0",
  },
});

export default useStyles;
