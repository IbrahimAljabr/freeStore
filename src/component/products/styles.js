import { makeStyles } from "@material-ui/core/styles";

const useStyles = makeStyles({
  container: {
    margin: "100px 0",
    background: "#f9f9f9",
  },
  box: {
    padding: "50px 0 ",
  },
});

export default useStyles;
